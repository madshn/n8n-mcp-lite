{
  "name": "n8n-mcp-lite",
  "version": "1.0.0",
  "description": "Token-optimized MCP Server for n8n workflows - 80%+ token reduction",
  "main": "dist/index.js",
  "bin": {
    "n8n-mcp-lite": "dist/index.js"
  },
  "type": "module",
  "scripts": {
    "build": "tsc && node -e \"const fs=require('fs');const p=require('path');const src=p.join('src','data');const dst=p.join('dist','data');if(fs.existsSync(src)){fs.mkdirSync(dst,{recursive:true});fs.readdirSync(src).forEach(f=>fs.copyFileSync(p.join(src,f),p.join(dst,f)));console.log('Copied data files to dist/')}\"",
    "build:nodes": "tsx scripts/build-nodes.ts",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts"
  },
  "keywords": [
    "n8n",
    "mcp",
    "model-context-protocol",
    "ai",
    "workflow",
    "automation",
    "token-optimization"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.27.1",
    "express": "^4.22.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "^7.6.13",
    "@types/express": "^5.0.6",
    "@types/node": "^22.0.0",
    "better-sqlite3": "^12.6.2",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
